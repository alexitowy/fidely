// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase : {
    apiKey: "AIzaSyCLQUYQuIm_mn32nMamf8vmzCDsi9EGHlw",
    authDomain: "fidelity-back.firebaseapp.com",
    projectId: "fidelity-back",
    storageBucket: "fidelity-back.appspot.com",
    messagingSenderId: "424791863353",
    appId: "1:424791863353:web:4e9bd8729f85502c2b5de8",
    measurementId: "G-W11621PX6N"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
